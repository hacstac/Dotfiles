[ -n "$PS1" ] && source ~/.bash_profile;

[ -f ~/.fzf.bash ] && source ~/.fzf.bash
. "$HOME/.cargo/env"

[[ -s "/home/akashrajvanshi/.gvm/scripts/gvm" ]] && source "/home/akashrajvanshi/.gvm/scripts/gvm"
